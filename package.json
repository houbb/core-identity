{
  "name": "core-identity",
  "private": true,
  "version": "0.1.0",
  "description": "Core Platform Identity - Identity and Access Management",
  "scripts": {
    "dev:web": "cd core-identity-web && npm run dev",
    "dev:admin": "cd core-identity-admin-web && npm run dev",
    "build": "cd core-identity-web && npm run build && cd ../core-identity-admin-web && npm run build",
    "test": "cd core-identity-web && npm run test && cd ../core-identity-admin-web && npm run test"
  }
}